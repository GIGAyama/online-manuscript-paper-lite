# ロールアウト記録 ── online-manuscript-paper-lite

GIGA Standard v5 の `/rollout` を当てた記録。
**他のリポジトリにも効く知見**を中心に残す。

- 対象：`GIGAyama/online-manuscript-paper-lite`（A型・単一HTML完結 → ビルドあり）
- 実施：2026-08
- 作業環境の制約：`gigayama.github.io` と `script.google.com` へは到達できない。
  CDN（jsdelivr / unpkg / tailwindcss / Google Fonts）へも出られないため、
  **学校のフィルタリングと同じ状態で測れた**（利点として使った）。npm レジストリへは出られる。

---

## 1. このリポジトリで直したこと（要約）

| フェーズ | 内容 |
|---|---|
| P0 | LICENSE / .gitignore / dependabot / CI（`pull_request` でも動く）を新設 |
| P0.5 | **ブラウザ内 Babel を廃止。CDN 実行コード 5本 → 0本。初回 JS 3,284KB → 254KB** |
| P1 | 100dvh・visualViewport・safe-area・clamp・タップ44px・reduced-motion・forced-colors・コントラスト17→0件・ふりがなの色・PWA一式・CSP |
| P2 | favicon 248KB → 4.3KB、maskable のセーフゾーン外 0.73% → 0.11% |
| P3 | MANUAL.md / AUDIT.md / README 追補、組版ロジックの切り出しとテスト |
| P4 | 品質ゲート（`npm run check`）と、**ゲート自体の自己テスト**（わざと壊して落ちることを見る） |

数字は `AUDIT.md` に全部載せてある。

---

## 2. 横断で効く知見（他リポジトリでも同じことが起きる）

### 2-1. ⚠️ 標準 §7-2 のコントラスト測定スニペットに誤りがある

```javascript
// §7-2 に載っている形
const d = ctx.getImageData(0, 0, 1, 1).data;
const a = d[3] / 255;
return a === 0 ? [0,0,0,0] : [d[0]/a, d[1]/a, d[2]/a, a];   // ← ここ
```

**Chrome の `getImageData` は「アルファを乗算していない」値を返す。**
実測：`rgba(255,255,255,0.9)` を塗って読むと `[255, 255, 255, 230]`。
つまり RGB はすでに素の色なので、`a` で割ると**明るくなりすぎる**。

このリポジトリでは、文字数の帯（`bg-white/90`）の背景が **rgb(297,297,295)** と算出され、
本当は **3.19**（基準未満）の比が **4.45** と出て、**見逃すところだった。**

- 直し方：割らずにそのまま返す（`[d[0], d[1], d[2], a]`）
- 合成は別に行う（`fg*a + bg*(1-a)`）
- ついでに、輝度計算に入る値は 0〜255 に丸めておく

**同じスニペットを写して使っているリポジトリは、コントラストの数字を測り直す必要がある。**

### 2-2. プレースホルダは通常の走査に出てこない

要素の子テキストを見る走査では、`placeholder` 属性の文字は**1件も出ない**。
別に `getComputedStyle(el, '::placeholder')` で測る必要がある。

このリポジトリでは、走査で 0件になったあとに 4件見つかった。

| 文言 | 色 | 比 |
|---|---|---:|
| 「だいめい」「がくねん・くみ」「なまえ」 | `#9ca3af`（Tailwind 既定） | **2.54** |
| 「ここをクリックして、さくぶんをかいてください...」 | `#9ca3af` | **1.71** |

いちばん最初に児童が読む文が、いちばん読めない形になっていた。

### 2-3. `::placeholder` だけでは Tailwind に負ける

Tailwind の preflight は `input::placeholder, textarea::placeholder`（詳細度 0,0,2）で色を当てる。
`::placeholder`（0,0,1）で上書きしようとしても**効かない**。同じ形で書くこと。

```css
input::placeholder,
textarea::placeholder { color: #475569; opacity: 1; }
```

`#64748b`（slate-500）では、罫線つきの入力欄（下じきが `#cbd5e1`）で **3.2** にしかならない。
`#475569`（slate-600）にすると罫線の上でも 4.5 を満たす。

### 2-4. 暖色テーマは amber-700 まで濃くしないと両側で届かない

| 使い方 | 色 | 比 |
|---|---|---:|
| `bg-amber-500` に白文字 | `#f59e0b` | **2.15** |
| 白地に `text-amber-600` | `#d97706` | **3.19** |
| **白地に `text-amber-700`** | `#b45309` | **4.60** |
| **`bg-amber-700` に白文字** | `#b45309` | **4.60** |

`#b45309` は面にも文字にも使えて、色の印象はほとんど変わらない。
橙系を使っている教育アプリは、**500 と 600 を全部 700 にする**のがいちばん早い。

### 2-5. CSP を入れると Playwright の `addScriptTag` が弾かれる

```
Refused to execute inline script because it violates the following
Content Security Policy directive: "script-src 'self' file:"
```

これは**CSP が効いている証拠**なので、無効化してはいけない。
検査スクリプトは `page.evaluate(source)`（CDP 経由）で流し込む。

なお **CSP の `content` 属性を正規表現で取り出すときは引用符を後方参照で合わせる。**
`content=["']([\s\S]*?)["']` と書くと、値の中の `'self'` の `'` で切れて
`script-src` を取り出せず、**何も見ないまま合格になる**（このリポジトリで実際に起きた。
ゲートの自己テストで見つかった）。

### 2-6. Service Worker の更新は「待機に入るまで待って」から放置する

`reload()` の直後に `registration.waiting` を見ても、まだ `null` のことがある。
そこで判定すると「更新の案内が出ない」という**誤った不合格**になる。

正しい順序：**待機に入るまで待つ → そこから3秒放置 → まだ待機のままか**。
本題は「勝手に切り替わらないこと」なので、放置する側が本番。

### 2-6b. `setOffline(true)` は Service Worker の fetch を止めない

**これに気づかないと、オフライン関連の測定が全部「何も測っていない」ことになる。**

Playwright の `context.setOffline(true)` を入れても、
**Service Worker 自身が出す `fetch()` はそのままサーバーへ届く。**
そのため、

- 「圏外で起動する」→ 実際にはネットワークから index.html を取っていた
- 「本体の控えを消したら offline.html が出る」→ **出ない**（ネットワークから本体が返るため）

という結果になる。実測でも、キャッシュから `/` と `/index.html` を確かに消して
`caches.match('./index.html')` が `false` になった状態で、
それでも本体が 200 で表示された（`fromServiceWorker: true` のまま）。

**サーバーのプロセスそのものを止めるのが、本当の圏外。**
止めてから測ったら、圏外起動も `offline.html` も期待どおりに動いた。

### 2-6c. 前回の測定サーバーが残っていると、直したはずの CSS が直って見えない

測定が途中で落ちるとポートを掴んだままのサーバーが残る。
次の測定は**サーバーの起動に黙って失敗し**、ブラウザは前回の測定用ディレクトリを見にいく。
「置換したはずのプレースホルダ色が反映されない」「更新の案内が出ない」と、
アプリ側を疑って時間を溶かした。

測定ツールは**使う前にポートが空いていることを確かめ、塞がっていたら止まる**ようにした。

### 2-7. maskable は下地の色を「絵から読む」

§3-7 は「単色のグラデーションを敷くと角丸四角の輪郭が薄い影として残る」と書いているが、
それは**元の下地に陰影がある場合**の話。このリポジトリの下地は一様な `#fafafa` だったので、
同じ色を端まで塗るだけで継ぎ目は出なかった。

逆に、**元の絵をぼかして下地にする方法は失敗した**（ペンの青と帯の黄が四隅ににじみ、
アイコンとして見苦しくなった）。まず下地の色を実際に読み、
一様ならその色、陰影があるなら下地だけを引き伸ばす、の順で判断するのがよい。

絵の縮小率は **0.68** で、セーフゾーン外の中身が 512/192 とも 0.2% を切った
（0.74 では 512 が 0.23%、192 が 0.49% で足りない。**192 のほうが縁のアンチエイリアスで不利**）。

### 2-8. 品質ゲートは「自分自身」に反応する

`localStorage.clear()` を探す検査は、**その検査のソースそのもの**に反応する。
`scripts/` `tools/` `tests/` を走査対象から外すこと（配信されるコードだけを見る）。

### 2-9. ゲートを作ったら必ず「わざと壊して」通す

`tests/quality-gate.test.mjs` は、リポジトリの写しを 30 通りに壊して、
**狙った検査が落ちること**を確かめている。これをやったことで、
**検査そのものの不具合が 2 件**見つかった（2-5 の CSP と、
`safe-area-inset` を生成物の CSS で見ていて原本を消しても落ちなかった件）。

「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できない。

---

## 3. このフリートで次にやること

`@babel/standalone` を使っている残りのリポジトリには、
このリポジトリの `tools/build.mjs` と `scripts/` がほぼそのまま使える。

```bash
# 横断で数える
grep -rln "babel/standalone" $(git ls-files '*.html')
grep -rln "cdn.tailwindcss.com" $(git ls-files '*.html')
grep -rn "text-amber-500\|text-amber-600\|bg-amber-500" $(git ls-files '*.html' '*.jsx' '*.css')
grep -Ln "startsWith" $(git ls-files '*sw.js')
grep -rn "d\[0\] */ *a" $(git ls-files '*.mjs' '*.js')   # §7-2 の誤ったスニペットの写し
```

`online-manuscript-paper` 系は `-lite` / 本体 / `-pro` / `Online-Publisher-pro` の
4系統あり、**どれを正本にするかは人間の判断待ち**（標準 Part V の「人間が決めること」）。
このリポジトリは 4系統のうち `-lite` だけを直してある。
他の3つに同じ組版コード（禁則処理）が入っている場合、
`src/genko-parser.js` をそのまま持っていける形にしてある。

---

## 4. 測っていないもの

- **本番（`gigayama.github.io`）での確認**。プロキシで到達できないため、
  ローカルの静的サーバー（`tools/measure/serve.mjs`）で測った。
  `manifest` の絶対パス（`/online-manuscript-paper-lite/`）が正しく効くかは、
  公開後に1回だけ実機で確かめてほしい。
- **実機の iPad / Chromebook**。ヘッドレス Chromium の各画面サイズで測っている。
  iOS Safari 固有の挙動（ホーム画面追加、ITP による消去）は未計測。
- **実際の印刷結果**。印刷 CSS は入れてあるが、紙に出したものは見ていない。
- **Google Fonts が届く状態での表示**。作業環境からは出られないため、
  端末側フォントにフォールバックした状態で測っている
  （＝フィルタリングされた学校と同じ条件）。
