# GIGA Standard v5 監査：オンライン原稿用紙 Lite

- 対象：`GIGAyama/online-manuscript-paper-lite`
- 型：**A型（単一HTML完結）** … ただし JSX と Tailwind のためビルド工程を追加した
- 実施：2026-08
- 測り方：
  1. 静的に読む（`npm run check` … `scripts/check-project.mjs`）
  2. **実ブラウザで開いて測る**（`npm run measure` … `tools/measure/measure.mjs`、Chromium 実機）
  3. **操作して測る**（入力・タブ切替・モーダルを開いた状態でも走査）
  4. **PWA の挙動を測る**（登録・初回リロード・更新・オフライン・他アプリのキャッシュ）

**「未計測」は ✅ ではない。測っていないものは「未計測」と書いた（最後の節）。**

---

## 0. 改修前の姿（推測ではなく実測）

改修前の `index.html` は CDN から React・Babel・Tailwind・SweetAlert2・アイコンフォントを
読む作りだった。作業環境からはこれらの CDN へ出られないため、
**そのまま開くと画面が真っ白**になる（＝学校のフィルタリングで起きることそのもの）。

数字を比べるためには改修前も同じ土俵で測る必要があるので、§7-4 の方式で
**検査用の複製だけ**を作った（npm から同じ版を取り、同じパスに並べ、
Tailwind は同じ v3 エンジン・同じ設定で CSS を生成）。リポジトリには手を入れていない。
Google Fonts はわざと塞がれたままにして、フィルタリングされた学校と同じ条件で測っている。

| 項目 | 改修前 | 改修後 |
|---|---:|---:|
| コントラスト基準未満 | **17 件** | **0 件** |
| 案内文（プレースホルダ）の基準未満 | **4 件** | **0 件** |
| タップ領域 44px 未満 | **7 件** | **0 件** |
| 320px 幅での横スクロール | 0 件 | 0 件 |
| JS エラー | 0 件 | 0 件 |
| CSP 違反 | （CSP なし） | 0 件 |
| 初回に必要な JS | **3,284 KB** | **254 KB** |
| CDN から取る実行コード | **5 本** | **0 本** |
| 初回に取る資産の合計（JS + CSS + アイコンフォント + HTML） | **3,547 KB** | **324 KB** |

> 改修前の数字は npm から同じ版を取った**実バイト**である。
> ただし `cdn.tailwindcss.com` だけは作業環境から取得できないため**未計測**で、
> この合計には入っていない。**実際にはこれよりさらに大きい。**

改修前の内訳（実バイト）：`@babel/standalone` 3,067.5KB ／ `react-dom` 128.7KB ／
`sweetalert2` 77.3KB ／ `react` 10.5KB ／ `bootstrap-icons.css` 83.9KB ＋ `woff2` 127.5KB ／
`index.html` 51.5KB。
**`@babel/standalone` の役目は「ブラウザの中で JSX を翻訳すること」なので、開くたびに全部やり直していた。**

コントラストで実際に見つかっていたもの（抜粋、白地・実測）：

| 文言 | 色 | 面 | 比 |
|---|---|---|---:|
| 「かく」「原稿用紙をみる」（タブ） | 白 | `#f59e0b` | **2.15** |
| 「Lite」バッジ | 白 | `#f59e0b` | **2.15** |
| ふりがな「げんこうようし」（タブの上） | `#64748b` | `#f59e0b` | **2.22** |
| ふりがな「ほぞん」（保存するボタンの上） | `#64748b` | `#334155` | **2.18** |
| 「オンライン原稿用紙」（見出し） | `#d97706` | 白 | **3.19** |
| 「印刷する」 | `#d97706` | `#fffbeb` | **3.07** |
| プレースホルダ「ここをクリックして、さくぶんをかいてください...」 | `#9ca3af` | 罫線 `#cbd5e1` | **1.71** |

**ふりがなが要るのは低学年の児童である。いちばん読めなくて困る人が、いちばん読めない形になっていた。**

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama を新規作成 |
| A2 | .gitignore | ✅ | `node_modules/` `dist/` `.clasp.json` `.env` `.DS_Store` を新規作成 |
| A3 | dependabot.yml | ✅ | npm と github-actions を monthly で新規作成 |
| A4 | README / MANUAL / AUDIT | ✅ | MANUAL.md・AUDIT.md・ROLLOUT.md を新規作成、README に構成とビルド手順を追記 |
| A5 | CI | ✅ | `push`(main) と **`pull_request` の両方**で `build → 生成物の差分 → check → test` |

---

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ✅ | `script-src 'self' file:`。**実ブラウザで CSP 違反 0 件・JS エラー 0 件**。CSP が効いていることは、検査スクリプトの `addScriptTag` が実際に弾かれることで確認（`Refused to execute inline script`） |
| B1b | 読み込み失敗 | ✅ | 0 件。コンソールに出る 21 件はすべて Google Fonts が届かないことによるもので、**わざと塞いだまま測っている**（フィルタリングされた学校と同じ条件）。字の形が変わるだけで動作に影響しない |
| B2 | 秘密情報・IDの直書きなし | ✅ | サーバーと通信しない設計。鍵らしき文字列 0 件 |
| B3 | OAuthスコープ最小 | — | 該当なし（GAS を使っていない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` は Service Worker への `{type:'SKIP_WAITING'}` のみ |
| B5 | サーバー側5段ガード | — | 該当なし（サーバーが無い。入力は端末内で完結し、送信しない） |
| B6 | CDN から取る実行コードが 0 | ✅ | **5本 → 0本**（React / ReactDOM / SweetAlert2 を `vendor/` へ、アイコンは SVG 埋め込み、Tailwind と JSX はビルド時に生成） |
| B7 | 残る外部資産に SRI と版の固定 | — | 実行コードの外部読み込みが 0 になったため SRI の対象が無い。残る外部参照は Google Fonts（スタイルシート）だけで、これは版を固定できず、また**止まっても字の形が変わるだけで動作に影響しない**ため付けない |

**インラインを全部外した。** CSP を入れると `<script>` の直書きと `onclick=` は動かなくなる。
本体を `js/app.js` へ、インストールの捕捉を `install-hook.js` へ切り出し、
`onclick=` は使っていない（React の `onClick` はインライン属性ではない）。
`'unsafe-inline'` は `style-src` にだけ付けた（React と SweetAlert2 が `style` 属性を書くため）。

`frame-ancestors` は `<meta>` では無視されるので書いていない。理由を `index.html` のコメントに残した。

---

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 該当なし（GAS を使っていない） |
| C2 | 自動復旧 | ✅ | 保存内容が壊れていても `try/catch` で捨てて起動する（既存の作り） |
| C3 | pagehide で記録確定 | ✅ | **新規**。1秒待つ自動保存だけでは Chromebook のタブ破棄で直前の数文字が消える |
| C4 | 通信失敗時のリトライと明示 | ✅ | 通信しない。圏外時は Service Worker が手元の控えを返し、本体が無ければ `offline.html` |
| C5 | localStorage.clear() を使っていない | ✅ | 自アプリのキー `genko_lite_v2` のみを読み書き |

---

## D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | もとから入っていた |
| D2 | 100dvh を使用 | ✅ | `.app-shell` を `100dvh` に。さらに **`visualViewport` の高さを `--vvh` に流し込む**（原稿用紙はキーボードで入力欄が隠れると機能が消えるため） |
| D3 | safe-area-inset を適用 | ✅ | **新規**。`.app-shell` の左右と、下部の要素（フッター・更新の帯）に `env(safe-area-inset-*)` |
| D4 | clamp() による fluid type | ✅ | **新規**。本文・見出し・**児童が打つ欄**に `clamp()` |
| D5 | Canvas に DPR 補正 | — | 該当なし（描画に Canvas を使っていない。原稿用紙は DOM で組んでいる） |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **320×568 で 0 件**（初期表示・入力あり・プレビュー・設定モーダルの4画面） |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` は無い。PNG は下表のとおり全部 30KB 未満 |
| D8 | **コントラスト 4.5:1 以上** | ✅ | **17 件 → 0 件**（4画面 × 4画面サイズを実ブラウザで走査） |
| D8b | **案内文（プレースホルダ）** | ✅ | **4 件 → 0 件**（要素の子テキストではないので別に測る必要がある） |
| D9 | **タップ領域 44px 以上** | ✅ | **7 件 → 0 件**（`::after` 込みで実測。入力欄は `min-height`、ラジオは囲みの `<label>`） |
| D10 | prefers-reduced-motion | ✅ | **新規**。`.01ms`（`0` にすると `fill-mode: forwards` が壊れてモーダルが消える） |
| D11 | forced-colors | ✅ | **新規** |
| D12 | 提示モード | — | 該当なしと判断。児童が1人1台で書く道具で、一斉に提示する画面ではない。プレビューは画面の高さに合わせて自動で拡大し、ブラウザの拡大も禁止していない |
| D13 | 印刷CSS | ✅ | もとからあった A4横・二段組を維持。**あとから足した固定要素（更新の帯）も消えるよう、入れ子の深さに依存しない書き方に直した**。手順は MANUAL.md に記載 |
| D14 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` は無い |

### 配色の変更（色相は変えず、1〜2段濃くした）

| 使い方 | 前 | 後 | 比（前 → 後） |
|---|---|---|---|
| 見出し・「印刷する」・文字数 | `#d97706` (amber-600) | `#b45309` (amber-700) | 3.19 → **4.60**（白地） |
| タブの選択中・「Lite」・アイコンの地 | `#f59e0b` (amber-500) の上に白 | `#b45309` の上に白 | 2.15 → **4.60** |
| ふりがな（色のついた面の上） | `#64748b` 決め打ち | 親から継がせる | 2.18〜2.22 → **面と同じ比（4.6 以上）** |
| プレースホルダ | `#9ca3af`（Tailwind 既定） | `#475569` | 1.71〜2.54 → **4.5 以上** |
| ラジオの選択色 | `teal-600` | `teal-700` | 面の色。文字ではないが揃えた |

ふりがなは**1か所ずつ潰していない。** 白地の既定値だけ与え、
ボタン・リンク・`bg-*` の面の上ではまとめて親から継がせている。

---

## E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url | ✅ | `"./"` → `/online-manuscript-paper-lite/`（`id` は新規追加） |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | `maskable-192` と `apple-touch-icon` を新規作成。**透明画素 0.00%** |
| E3 | beforeinstallprompt を head 最上部の外部ファイルで捕捉 | ✅ | `install-hook.js` を新規作成し、`<head>` の最初のスクリプトとして同期読み込み |
| E4 | インストールボタン | ✅ | 案内できるときだけ表示（既存の作りを維持） |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | **実測**：別アプリの `other-app-static-v1` を置いてから版を上げ、**`other-app-static-v1` は残ったまま**（消えたのは `genko-lite-static-v2` だけ） |
| E6 | sw.js が localStorage に触れていない | ✅ | 触れていない |
| E7 | 更新通知（押すまで切り替わらない） | ✅ | **実測**：待機に入ってから3秒放置しても **待機のまま**。案内の帯 **出た** |
| E8 | 初回訪問で勝手にリロードしない | ✅ | **実測**：まっさらな状態で開き、画面遷移 **1 回** |
| E9 | Service Worker が実際に登録されている | ✅ | **実測**：`getRegistration()` → **登録されている** |
| E10 | offline.html | ✅ | 新規作成。`<script>` なし・外部資産なし。**実測**：本体の控えを消して圏外にすると **offline.html が出た** |
| E11 | APP_VERSION を更新した | ✅ | `genko-lite-v1` → 接頭辞 `genko-lite-` + `APP_VERSION = 'v2'` |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | **512: 0.73% → 0.11%／192: （無かった）→ 0.13%**（画素を数えて確認） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | MANUAL.md §2 に画面の場所まで記載 |

### sw.js で直した中身

| 直したこと | 直す前に何が起きるか |
|---|---|
| **削除の条件を `!key.startsWith(CACHE_VERSION)` → `key.startsWith('genko-lite-')` に** | 版を上げた瞬間に、**同じオリジンにある他のアプリのキャッシュを全部消す**。他のアプリがオフラインで起動しなくなる |
| `install` の `skipWaiting()` を外した | 児童が作文を打っている最中に中身が入れ替わり、打ちかけの文が消える |
| `controllerchange` を「押したときだけ」受けるようにした | **初回訪問が必ず1回リロードされる**（打ちかけの入力が消える） |
| 更新の案内（`あたらしい ばんが あります` / `さいしんに する`）を追加 | 新しい版があることが利用者に伝わらない |
| 登録に `document.readyState === 'complete'` の分岐を入れた | React の effect は描画のあとに走るため、`load` を待つだけだと**登録されない** |
| CDN ホストのキャッシュ設定を削除 | CDN を使わなくなったため不要 |
| `offline.html` を先読みに追加 | 本体の控えが無いときに何も出せない |

---

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / role | ✅ | アイコンのみのボタン（設定・閉じる）に `aria-label`、アイコンは `aria-hidden`。入力欄に `aria-label`。更新の帯は `role="status"` |
| F2 | モーダルに role="dialog"・Esc で閉じる | ✅ | `role="dialog" aria-modal="true" aria-labelledby`。Esc（既存）に加え **Tab をモーダルの中に閉じ込め**、閉じたら元の場所へフォーカスを戻す |
| F3 | キーボードのみで全機能に到達 | ✅ | すべて素の `button` / `input`。`:focus-visible` に 3px の輪郭を付けた |
| F4 | rt の色を決め打ちしていない | ✅ | 決め打ちをやめ、色のついた面ではまとめて継がせる。`<rp>` を添えて二重読みを防ぐ |
| F5 | 初回JS 300KB以下 | ✅ | **254 KB**（前：3,284 KB） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `src/app.jsx`（約 870 行）。`index.html` は 920行 → 93行 |
| F7 | 見出しの順序 | ✅ | `h1`（アプリ名）→ `h2`（モーダルの題）→ `h3`（設定の項目）。改修前は `h1` が無く `h3`/`h4` だけだった |

### 大きさの内訳（実バイト）

| ファイル | 大きさ |
|---|---:|
| `vendor/react-dom.js` | 128.7 KB |
| `vendor/sweetalert2.js` | 77.3 KB |
| `vendor/react.js` | 10.5 KB |
| `js/app.js` | 29.9 KB |
| `js/icons.js` | 6.4 KB（13個の SVG） |
| `install-hook.js` | 1.0 KB |
| **初回に必要な JS 合計** | **254 KB**（目標 300KB 以下。前：3,284 KB） |
| `css/style.css` | 24.3 KB |
| **総アセット** | **325 KB**（目標 1MB 以下） |

### 画像（パレット PNG 化）

| ファイル | 前 | 後 |
|---|---:|---:|
| `favicon.png`（512×512 フルカラー → 256×256 パレット） | 248.1 KB | **4.3 KB** |
| `icons/icon-512.png` | 16.1 KB | **9.2 KB** |
| `icons/icon-192.png` | 4.8 KB | **4.0 KB** |
| `icons/maskable-512.png` | 12.0 KB | **9.0 KB** |
| `icons/maskable-192.png` | （無し） | 2.5 KB |
| `icons/apple-touch-icon.png` | （無し・icon-192 を流用） | 3.1 KB |
| **合計** | **281.0 KB** | **32.1 KB** |

---

## G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠 | — | 該当なし。学習の記録を取るアプリではない（作文を書いて印刷する道具） |
| G2 | 中断記録・5分ルール | — | 同上 |

---

## H. 品質ゲート

`npm run check` … 共通の検査（`scripts/lib/project-quality.mjs`）と
Part I の検査（`scripts/lib/giga-v5-checks.mjs`）を `scripts/check-project.mjs` が合成する。
フリート共通の「正本」が配られたら、前者をバイト単位で置き換えられる形にしてある。

**ゲートは「わざと壊して」通ることを確かめてある**（`tests/quality-gate.test.mjs`。
リポジトリの写しを 30 通りに壊し、狙った検査が落ちることを見る）。
この確認をしたことで、**検査そのものの不具合が 2 件**見つかった。

| 見つかった不具合 | 中身 |
|---|---|
| `CSP_PRESENT` が何も見ていなかった | `content=["']…["']` で取り出すと、値の中の `'self'` の `'` で切れて `script-src` を読めない。引用符は後方参照で合わせる |
| `SAFE_AREA` が原本を見ていなかった | 生成物の `css/style.css` を見ていたため、原本から消しても落ちなかった |

検査は 39 件（合格 38 / 対象外 1）。

---

## I. 測っていないもの（「未計測」であって ✅ ではない）

- **本番（`gigayama.github.io`）での確認**。作業環境から到達できない（プロキシ 403）。
  `manifest` の絶対パス `/online-manuscript-paper-lite/` が実際の配信先と一致するかは、
  公開後に1回だけ実機で確かめてほしい。**ここが違うとインストールが別アプリ扱いになる。**
  - なお `"./"` → 絶対パスの変更で**同一性は変わらない**。
    `/online-manuscript-paper-lite/manifest.webmanifest` にある `"./"` の解決結果は
    `/online-manuscript-paper-lite/` そのものなので、すでにインストール済みの端末で
    別アプリになることはない（`id` の新規追加も、省略時の既定値＝`start_url` と同じ値にしてある）。
- **実機の iPad・Chromebook・電子黒板**。ヘッドレス Chromium を
  1366×768 / 810×1080 / 375×667 / 320×568 の4サイズで測っている。
  iOS Safari 固有の挙動（ホーム画面追加の見え方、ITP による localStorage の消去、
  apple-touch-icon の見え方）は未計測。
- **実際に紙へ印刷した結果**。印刷 CSS は保っているが、プリンタ出力は見ていない。
- **Google Fonts が届く状態での表示**。作業環境から出られないため、
  端末側フォントにフォールバックした状態で測っている（＝フィルタリングされた学校と同じ条件）。
  字の形が変わるだけで、レイアウトの破綻が無いことは確認済み。
- **ソフトキーボードが出た状態の実測**。`visualViewport` に追従する仕組みを入れたが、
  ヘッドレスではキーボードが出せないため、実機での確認は未実施。
- **他アプリと同居した本番オリジンでの Service Worker の挙動**。
  ローカルに別名のキャッシュを置いて確かめており、本番の他アプリでは確かめていない。
